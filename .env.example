# Environment Variables Configuration
# Copy this file to .env for development or .env.production for production

# Spring Boot Configuration
SPRING_PROFILES_ACTIVE=dev

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-password
MONGO_DATABASE=deal_pipeline_db
MONGO_APP_PASSWORD=change-this-app-password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-minimum-256-bits-long
JWT_EXPIRATION=3600000

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:80

# Development Settings
# For development, you can use these default values
# For production, use secrets files and override these values
